import './_setup';
import { ApolloModule } from '../src/ApolloModule';
import { Apollo } from '../src/Apollo';
import { SelectPipe } from '../src/SelectPipe';
describe('ApolloModule', function () {
    var metadata = Reflect.getMetadata('annotations', ApolloModule)[0];
    test('should be defined', function () {
        expect(ApolloModule).toBeDefined();
    });
    test('should provide Apollo', function () {
        expect(metadata.providers).toContain(Apollo);
    });
    test('should declare SelectPipe', function () {
        expect(metadata.declarations).toContain(SelectPipe);
    });
    test('should export SelectPipe', function () {
        expect(metadata.exports).toContain(SelectPipe);
    });
});
//# sourceMappingURL=ApolloModule.spec.js.map